import {ViewScale} from 'utils';
import {StyleSheet} from 'react-native';
export default styles = StyleSheet.create({
  rootContainer: {
    marginTop: ViewScale(20),
  },
  pickerContainer: {marginTop: ViewScale(10)},
  selectBox1text: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: ViewScale(20),
  },
});
